{
    "name": "itongquiz-api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:migrate": "wrangler d1 execute itongquiz-db --file=schema.sql --remote",
        "db:migrate:local": "wrangler d1 execute itongquiz-db --file=schema.sql --local"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20241218.0",
        "typescript": "^5.7.0",
        "wrangler": "^4.72.0"
    }
}
