# Audit Report - 2026-03-25

## Summary
- 🔴 Critical Issues: 1
- 🟡 Warnings: 2
- 🟢 Suggestions: 2

---

## 🔴 Critical Issues (Phải sửa ngay)
1. **Lộ lọt API Key (Hardcoded Secret)**
   - **File:** `src/services/geminiService.ts` (Dòng 1079)
   - **Nguy hiểm:** Đang có một chuỗi khóa API dạng `[REDACTED-COMPROMISED-KEY]` được ghi trực tiếp vào mã nguồn dự án làm giá trị dự phòng (fallback). Bất cứ ai truy cập mã nguồn hoặc soi file mã hóa JS trên trình duyệt đều có thể lấy trộm key này để dùng chùa (gây tốn tiền) hoặc phá hoại dịch vụ.
   - **Cách sửa:** Chuyển key này hoàn toàn vào file `.env` (hoặc `.env.local`), TUYỆT ĐỐI không ghi cứng vào code.

## 🟡 Warnings (Nên sửa)
1. **Rác Hệ Thống & TS Error (Dead Code / Outdated folder)**
   - **Vị trí:** Thư mục `backup_20251226/TeacherDashboard.tsx`
   - **Vấn đề:** Thư mục backup cũ kỹ chưa được xóa nhưng vẫn bị trình dịch TypeScript quét qua. Nó đang chứa các dòng code import sai (`Cannot find module '../geminiService'`) gây lỗi đỏ mỗi khi chạy Build Type Check.
   - **Cách sửa:** Xóa thư mục backup này đi vì mình đã quản lý version bằng Git rồi. Không nên tạo thư mục backup thủ công trong source code.

2. **Cảnh báo React Rendering (Sử dụng `key={index}`)**
   - **Vị trí:** `src/components/ExplanationModal.tsx` và một số chỗ khác.
   - **Vấn đề:** React luôn khuyến cáo KHÔNG DÙNG chỉ số `index` (0,1,2..) để làm thẻ `key` khi render danh sách linh động. Việc này khiến React nhận diện sai thành phần khi mảng dữ liệu bị thay đổi (thêm/xóa), dẫn đến UI bị nhập nhằng hoặc giật lag.
   - **Cách sửa:** Dùng ID của phần tử (vd: `item.id`) để làm `key={}`.

## 🟢 Suggestions (Tùy chọn)
1. **Component quá dài (God Component - Code Complexity)**
   - **Vị trí:** `src/components/TeacherDashboard/CreateTab.tsx`
   - **Vấn đề:** File này đang chứa quá nhiều logic (hơn 1000 dòng code), xử lý vô vàn tác vụ từ Data fetching, State quản lý đến giao diện UI đồ sộ. Nó rất khó để những dev khác (hoặc chính anh sau này) dễ dàng bảo trì hay sửa lỗi.
   - **Cách sửa:** Tiến hành Refactor chia nhỏ component này thành các file con chuyên biệt (ví dụ: `PromptForm.tsx`, `QuizPreviewPanel.tsx` v.v).

2. **File JS Build Quá Lớn (Thiếu Lazy Load)**
   - **Vấn đề:** Khi build ra bản chạy trên mạng (Vercel), file tải màn hình `QuizPreview` hiện đang nặng tới **479 KB**. Trang web sẽ mở chậm lần đầu do người dùng phải tải qua mạng đống code này dù có thể họ chưa ấn vào tính năng tính điểm.
   - **Cách sửa:** Sử dụng chia cắt mã `React.lazy()` để làm nhẹ file tải trang gốc (Initial Bundle Size).

## Next Steps
Báo cáo này đã được hoàn thành dưới dạng file đánh giá kỹ thuật dành cho người điều hành dự án. Vui lòng chọn hướng đi tiếp theo.
